{
  "name": "retraction-optimizer",
  "version": "1.0.0",
  "description": "A tool to generate retraction DOE G-code patterns for 3D printers",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.0.0"
  }
}
